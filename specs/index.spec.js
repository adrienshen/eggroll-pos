const expect = require('chai').expect
// const app = require('../app.js');

describe('test travisci', () => {
  it('should return a string', () => {
    expect('ci with travis').to.equal('ci with travis');
  });
});

