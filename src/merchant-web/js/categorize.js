import React from "react";
import {Container, Form, Row, Col, Button} from "react-bootstrap";
import {DisplayOrder} from DisplayOrder;
import {TimeToPickup, TimeLeft} from TimeToPickup;

//Find way to get data
var orderlist = ;
orderlist.forEach(element => { element.confirmationTime
    
});